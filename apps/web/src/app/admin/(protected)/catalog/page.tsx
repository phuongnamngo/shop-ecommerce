import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function AdminCatalogStubPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Catalog</CardTitle>
        <CardDescription>Product / category / brand CRUD — sắp có.</CardDescription>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Coming soon
      </CardContent>
    </Card>
  );
}
